#include "pch.h"
#include "Camera.h"
#include "Scene.h"

void nc::Camera::Update(float dt)
{
}

//void nc::Camera::SetLookAt(const glm::vec3& eye, const glm::vec3& target, const glm::vec3& up, bool initialize)
//{
//	/*m_view = glm::lookAt(eye, target, up);
//	m_transform = glm::inverse(m_view);
//
//	if (initialize)
//	{
//		m_rotation = glm::quat_cast((glm::mat3)m_transform);
//	}*/
//}
